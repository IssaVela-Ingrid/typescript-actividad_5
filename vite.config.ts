// vite.config.ts
import { defineConfig } from 'vite';

export default defineConfig({
  base: '/IssaVela-Ingrid-typescript-actividad_5/', // ¡CRÍTICO! Asegúrate que sea exactamente el nombre de tu repositorio
});
